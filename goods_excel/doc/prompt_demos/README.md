# Prompt Demos

将按品类维护的标准样本标题放在本目录下，主流程会优先读取这里的文件作为 prompt 参考样本。

当前支持的文件名:

- `江苏特产.txt`
- `农副产品.txt`
- `苏超纪念品.txt`
- `工艺产品.txt`

规则:

- 每行一条样本
- 允许行尾带价格，读取时会自动去掉末尾价格
- 这些样本只用于约束命名颗粒度、品类边界、常见载体和价格带，不会被原样直接写回
- 主流程会提示模型“只参考结构，禁止照抄标题”

兼容性:

- 若本目录不存在对应文件，程序会回退读取项目根目录下的同名 `txt`
