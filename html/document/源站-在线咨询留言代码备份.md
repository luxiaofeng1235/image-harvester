# 源站在线咨询/留言代码备份（zgzonre.com）

- 源站：`https://www.zgzonre.com/`
- 备份时间：`2026-03-02`
- 目的：后续在新站嵌入“在线咨询/留言”能力时可直接复用

## 1. 已落地的原始文件

- 首页源码快照：`out/zgzonre/homepage.html`
- 侧边栏交互脚本：`out/zgzonre/rightmenu.js`
- 侧边栏样式：`out/zgzonre/sidebar02.css`
- 提取片段（首页侧边栏）：`out/zgzonre/snippet_sidebar_homepage.txt`
- 提取片段（咨询/留言事件）：`out/zgzonre/snippet_rightmenu_handlers.txt`

## 2. 源站首页中的侧边栏注入与配置代码（关键）

> 来源：`out/zgzonre/homepage.html`（约 532-592 行）

```html
<div id='layerCBEBB2153194013B26685B04DC2EF79E' type='sidebar' class='cstlayer' ...>
  <div class="wp-sidebar_content" ...>
    <link href="https://static.ysjianzhan.cn/website/plugin/sidebar/css/sidebar02.css?t=3" rel="stylesheet" type="text/css" />

    <script type="text/javascript">
      $(function(){
        var celancount= $('.wpsidebar02').length;
        if(!celancount) $("<div class=\"wpsidebar-fkf wpsidebar02\"><ul class=\"fkf\"> ... </ul></div>").appendTo('body');

        // 左侧参数（留言/咨询链接配置入口）
        $.sidebar_aleft = '1';
        $.sidebar_pathimg = 'https://static.ysjianzhan.cn/website/plugin/sidebar/images/';
        $.sidebar_menuclolr = '#6aa84f';
        $.sidebar_linkstr = $.parseJSON('{"help":{"linkurl":"","target":""},"message":{"linkurl":"","target":""}}');
      });
    </script>

    <script src="https://static.ysjianzhan.cn/website/plugin/sidebar/js/rightmenu.js?v=5" type="text/javascript"></script>
  </div>
</div>
```

## 3. 在线咨询/留言点击逻辑（源站 rightmenu.js）

> 来源：`out/zgzonre/rightmenu.js`（133-175 行）

```javascript
$('.wpsidebar02 li.smessage img.hover').click(function () {
  if ($.sidebar_linkstr.message.linkurl) {
    if($.sidebar_linkstr.message.target=='_blank'){
      var newwindow=window.open('about:blank');
      newwindow.location.href=$.sidebar_linkstr.message.linkurl;
      return '';
    }
    location.href=$.sidebar_linkstr.message.linkurl;
  }
  return false;
})

$('.wpsidebar02 li.shelp img.hover').click(function () {
  if ($.sidebar_linkstr.help.linkurl) {
    if($.sidebar_linkstr.help.target=='_blank'){
      var newwindow=window.open('about:blank');
      newwindow.location.href=$.sidebar_linkstr.help.linkurl;
      return '';
    }
    location.href=$.sidebar_linkstr.help.linkurl;
  }
  return false;
})
```

## 4. 当前源站首页状态确认

- 源站首页已加载 sidebar 插件与 `rightmenu.js`。
- `rightmenu.js` 内确实存在 `smessage`（留言）和 `shelp`（在线咨询）事件处理。
- 但首页当前注入的 `<li>` 结构主要是电话、微信二维码、回到顶部；
  `$.sidebar_linkstr.help/message` 目前是空链接（`""`）。

## 5. 后续嵌入新站时最小保留项

1. 保留 `sidebar02.css` 与 `rightmenu.js` 引入。
2. 保留 `$.sidebar_linkstr` 配置段，并填入实际链接。
3. 页面中存在对应 DOM 项（`li.shelp` / `li.smessage`）时，点击才会生效。

